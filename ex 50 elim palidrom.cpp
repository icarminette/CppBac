/*elim elem palindrom*/

#include <iostream>
using namespace std;

int main () {

    int n,v[100],c,i,inv,x;
    cin>>n;
    for (i=0; i<n; i++)
    cin>>v[i];

    /*for(i=1;i<n;i++) {
    z=v[i]
    inv=0;
    while (x!=0) {
        c=x%10;
        x=x/10;
        inv = inv*10+c;
    }

    if (inv==x)
    */
    for (i=0;i<n-1;i++)
        if(inv==x)
        v[i]


    cout<<pmax<<pmin;

    for (i=0;i<n;i++)
        cout<<v[i]<<" ";

}
